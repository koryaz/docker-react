language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t koryaz/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true koryaz/docker-react npm run test -- --coverage
